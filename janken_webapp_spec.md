# じゃんけんWebアプリ仕様書

## 企画名：0.5秒の勇者

### 反射神経 × じゃんけんRPG

---

## 1. コンセプト

通常のじゃんけんに、**RPGのHPバトル要素**と**反射神経によるダメージ補正**を組み合わせたWebゲーム。

プレイヤーはCPUと1対1で対戦する。じゃんけんに勝つと敵にダメージを与え、負けると自分がダメージを受ける。

特徴は、「SHOOT!」表示後にどれだけ早く手を選べるかでダメージ倍率が変わること。さらに、手ごとに基本ダメージが異なるため、反応速度だけでなく、どの手を選ぶかにも戦略性がある。

---

## 2. ゲーム概要

| 項目 | 内容 |
|---|---|
| ジャンル | じゃんけんバトルRPG |
| プレイ人数 | 1人 |
| 対戦相手 | CPU |
| 操作方法 | グー・チョキ・パーのボタンをクリック |
| 勝利条件 | 敵のHPを0にする |
| 敗北条件 | プレイヤーのHPが0になる |
| ステージ制 | なし |
| 特徴 | 反応速度によってダメージ倍率が変化する |

---

## 3. 基本ルール

### 3.1 ラウンド開始

「バトル開始」または「次のラウンド」ボタンを押すと、カウントダウンを表示する。

```txt
3
2
1
SHOOT!
```

「SHOOT!」表示と同時に以下を行う。

- 反応時間の計測を開始する
- CPUの手をランダムに決定する
- CPUの手を画面中央の敵側に先に表示する
- グー・チョキ・パーのボタンを表示し、有効化する

プレイヤーは「SHOOT!」表示後、1.0秒以内に以下のいずれかを選択する。

- グー
- チョキ
- パー

---

## 4. 勝敗判定

通常のじゃんけんルールで勝敗を判定する。

| プレイヤー | CPU | 結果 |
|---|---|---|
| グー | チョキ | 勝ち |
| グー | パー | 負け |
| チョキ | パー | 勝ち |
| チョキ | グー | 負け |
| パー | グー | 勝ち |
| パー | チョキ | 負け |
| 同じ手 | 同じ手 | あいこ |

---

## 5. HP仕様

プレイヤーと敵はそれぞれHPを持つ。

| キャラクター | 初期HP |
|---|---:|
| プレイヤー | 100 |
| 敵 | 100 |

HPは数値とHPバーで表示する。HPが0未満になった場合は0に補正して表示する。

| 条件 | 結果 |
|---|---|
| 敵HPが0以下 | プレイヤーの勝利 |
| プレイヤーHPが0以下 | プレイヤーの敗北 |
| 両者のHPが残っている | 次のラウンドへ進行 |

ゲーム終了後に「リスタート」を押すと、プレイヤーHP・敵HPを100に戻し、バトルログを空にして再開する。

---

## 6. 手ごとの性能

各手には、表示名・技名・基本ダメージを設定する。

| 内部値 | 手 | 技名 | 基本ダメージ | 性質 |
|---|---|---|---:|---|
| `rock` | グー | パワーパンチ | 30 | ハイリスク・ハイリターン |
| `scissors` | チョキ | スピードスラッシュ | 20 | バランス型 |
| `paper` | パー | バリアウェーブ | 15 | ローリスク・ローリターン |

### 6.1 手ごとの考え方

#### グー：パワーパンチ

勝つと大ダメージを与えられる。ただし、負けた時に受けるダメージも大きい。

#### チョキ：スピードスラッシュ

攻撃力とリスクのバランスがよい。安定して使いやすい標準型。

#### パー：バリアウェーブ

与えるダメージは小さい。ただし、負けた時の被ダメージも小さいため安全寄り。

---

## 7. 反応速度システム

### 7.1 反応時間の計測

「SHOOT!」が表示された瞬間を開始時間とする。プレイヤーが手を選んだ瞬間までの時間を秒単位で計測する。

```txt
反応時間 = ボタンを押した時刻 - SHOOT表示時刻
```

表示例：

```txt
反応速度：0.42秒
```

### 7.2 反応速度ランク

反応時間に応じて、ダメージ倍率を変化させる。

| 反応時間 | ランク | ダメージ倍率 | 内容 |
|---:|---|---:|---|
| 0.25秒以内 | PERFECT | 2.0倍 | 超反応 |
| 0.50秒以内 | GOOD | 1.5倍 | 素早い反応 |
| 1.00秒以内 | NORMAL | 1.0倍 | 通常反応 |
| 1.00秒超過 | MISS | なし | 出遅れ |

### 7.3 MISS判定

「SHOOT!」表示から1.0秒以内に手を選ばなかった場合はMISS扱いとする。

MISSの場合、じゃんけんの勝敗判定は行わず、プレイヤーが固定20ダメージを受ける。

```txt
MISS！反応が遅れた！
20ダメージを受けた！
```

---

## 8. ダメージ計算

### 8.1 プレイヤーが勝った場合

プレイヤーが出した手の基本ダメージに、反応速度倍率をかけた値を敵に与える。

```txt
与ダメージ = プレイヤーの手の基本ダメージ × 反応速度倍率
```

例：

```txt
プレイヤー：グー
CPU：チョキ
結果：勝ち
反応速度：0.42秒
ランク：GOOD
倍率：1.5倍

30 × 1.5 = 45ダメージ
```

### 8.2 プレイヤーが負けた場合

プレイヤーが出した手の基本ダメージに、反応速度倍率をかけた値を自分が受ける。

```txt
被ダメージ = プレイヤーの手の基本ダメージ × 反応速度倍率
```

例：

```txt
プレイヤー：グー
CPU：パー
結果：負け
反応速度：0.42秒
ランク：GOOD
倍率：1.5倍

30 × 1.5 = 45ダメージを受ける
```

### 8.3 あいこの場合

あいこの場合はダメージなし。

```txt
あいこ！あなた:グー / 敵:グー。ダメージなし。
```

### 8.4 MISSの場合

MISSの場合は、プレイヤーが固定20ダメージを受ける。

```txt
自分に 20ダメージ
```

---

## 9. ダメージ例

| 手 | 結果 | 反応ランク | 計算 | ダメージ |
|---|---|---|---|---:|
| グー | 勝ち | PERFECT | 30 × 2.0 | 60 |
| グー | 負け | PERFECT | 30 × 2.0 | 60 |
| チョキ | 勝ち | GOOD | 20 × 1.5 | 30 |
| チョキ | 負け | GOOD | 20 × 1.5 | 30 |
| パー | 勝ち | PERFECT | 15 × 2.0 | 30 |
| パー | 負け | PERFECT | 15 × 2.0 | 30 |
| どの手でも | あいこ | 任意 | なし | 0 |
| 未選択 | MISS | 1.00秒超過 | 固定 | 20 |

---

## 10. ゲームの流れ

### 10.1 初期表示

1. タイトル「0.5秒の勇者」を表示する
2. ルール説明を表示する
3. プレイヤーHPを100/100で表示する
4. 敵HPを100/100で表示する
5. 中央にプレイヤー・敵・手の結果表示エリアを表示する
6. 「バトル開始」ボタンを表示する
7. グー・チョキ・パーのボタンは非表示にする
8. リザルトとバトルログ欄を表示する

### 10.2 ラウンド開始

1. 「バトル開始」または「次のラウンド」ボタンを押す
2. 結果表示、手の画像、選択内容、ダメージ表示をリセットする
3. スタートボタンを非表示にする
4. 手の選択ボタンを無効化し、非表示にする
5. カウントダウンを `3 → 2 → 1 → SHOOT!` の順に表示する
6. `SHOOT!` 表示時にCPUの手をランダムに決定する
7. CPUの手を敵側にプレビュー表示する
8. `SHOOT!` 表示時点の時刻を記録する
9. 手の選択ボタンを有効化し、表示する
10. 1.0秒後に未選択ならMISS処理を行う

### 10.3 プレイヤー選択

1. プレイヤーがグー・チョキ・パーを選択する
2. ラウンドを非アクティブにする
3. 反応時間を計測する
4. 反応速度ランクを判定する
5. 事前に決定済みのCPUの手と勝敗判定する
6. ダメージを計算する
7. HPを更新する
8. リザルト、手の画像、ダメージ要約を画面に表示する
9. バトルログを先頭に追加する
10. 手の選択ボタンを非表示にする
11. ゲーム終了判定を行う

### 10.4 MISS処理

1. 「SHOOT!」表示から1.0秒が経過する
2. プレイヤーがまだ手を選んでいない場合、MISS判定にする
3. プレイヤーに固定20ダメージを与える
4. プレイヤー側を「未選択」として表示する
5. 敵側にはSHOOT時に決定したCPUの手を表示する
6. リザルト、ダメージ要約、バトルログを更新する
7. 手の選択ボタンを非表示にする
8. ゲーム終了判定を行う

### 10.5 ゲーム終了判定

ラウンド終了後、HPを確認する。

| 条件 | 表示 |
|---|---|
| 敵HPが0以下 | カウントダウン欄に「勝利！」を表示し、「リスタート」ボタンを表示 |
| プレイヤーHPが0以下 | カウントダウン欄に「敗北…」を表示し、「リスタート」ボタンを表示 |
| どちらもHPが残っている | カウントダウン欄を空にし、「次のラウンド」ボタンを表示 |

---

## 11. 画面構成

### 11.1 レイアウト

画面は3カラム構成とする。

| エリア | 内容 |
|---|---|
| 左カラム | ルール説明 |
| 中央カラム | プレイヤー・敵のHP、キャラクター画像、手の結果、操作ボタン、カウントダウン |
| 右カラム | リザルト、バトルログ |

画面幅が狭い場合は1カラムに切り替える。

### 11.2 表示要素

| 要素 | 内容 |
|---|---|
| タイトル | 0.5秒の勇者 |
| ルール説明 | 手ごとの基本ダメージ、反応速度倍率、MISS時の固定ダメージ |
| プレイヤーHP | 数値とHPバー |
| 敵HP | 数値とHPバー |
| キャラクター画像 | プレイヤー画像、敵画像 |
| 手の結果表示 | プレイヤーの手画像、敵の手画像、VS、ダメージ要約 |
| カウントダウン表示 | 3、2、1、SHOOT!、勝利！、敗北… |
| 手の選択ボタン | グー、チョキ、パー |
| 結果表示 | 勝敗、反応速度、ランク、あなたの手、敵の手、技、ダメージ |
| バトルログ | 最新の結果履歴を上に追加 |
| 次のラウンドボタン | 次の勝負へ進む |
| リスタートボタン | ゲームを最初からやり直す |

### 11.3 表示色

結果に応じてリザルト欄とバトルログの色を切り替える。

| 結果 | 表示色の意味 |
|---|---|
| 勝ち | 緑系 |
| 負け | 赤系 |
| あいこ | 黄系 |
| MISS | グレー系 |

---

## 12. 主な変数設計

```js
let playerHp = 100;
let enemyHp = 100;

const handData = {
  rock: { label: "グー", skillName: "パワーパンチ", damage: 30 },
  scissors: { label: "チョキ", skillName: "スピードスラッシュ", damage: 20 },
  paper: { label: "パー", skillName: "バリアウェーブ", damage: 15 }
};

let shootTime = 0;
let reactionTime = 0;
let speedRank = "";
let speedRate = 1;
let damage = 0;
let isRoundActive = false;
let currentEnemyHand = null;
```

---

## 13. データ設計

### 13.1 手の情報

```js
const handData = {
  rock: {
    label: "グー",
    skillName: "パワーパンチ",
    damage: 30
  },
  scissors: {
    label: "チョキ",
    skillName: "スピードスラッシュ",
    damage: 20
  },
  paper: {
    label: "パー",
    skillName: "バリアウェーブ",
    damage: 15
  }
};
```

### 13.2 手の画像

```js
const handImages = {
  rock: "images/gu.png",
  scissors: "images/choki.png",
  paper: "images/par.png"
};
```

### 13.3 反応速度ランク

```js
if (reactionTime <= 0.25) {
  speedRank = "PERFECT";
  speedRate = 2.0;
} else if (reactionTime <= 0.5) {
  speedRank = "GOOD";
  speedRate = 1.5;
} else if (reactionTime <= 1.0) {
  speedRank = "NORMAL";
  speedRate = 1.0;
} else {
  speedRank = "MISS";
  speedRate = 0;
}
```

---

## 14. 主な関数設計

### 14.1 startRound()

1ラウンドを開始する。

主な処理：

- ゲーム終了後のリスタートであればHPを100に戻し、バトルログを消去する
- リザルト表示をリセットする
- 手の結果表示をリセットする
- スタートボタンを非表示にする
- 手の選択ボタンを無効化し、非表示にする
- カウントダウンを開始する
- `SHOOT!` 表示時にCPUの手を決定する
- CPUの手をプレビュー表示する
- `shootTime` を記録する
- ラウンドをアクティブにする
- 手の選択ボタンを有効化し、表示する
- 1.0秒以内に選ばれなかった場合、MISS処理を行う

### 14.2 chooseHand(hand)

プレイヤーが手を選んだ時に実行する。

主な処理：

- ラウンド中でなければ処理しない
- ラウンドを非アクティブにする
- 反応時間を計測する
- 反応速度ランクを判定する
- `currentEnemyHand` をCPUの手として使用する
- 勝敗を判定する
- ダメージを計算する
- HPを更新する
- リザルト表示を更新する
- 手の画像とダメージ要約を更新する
- 結果に応じてリザルト欄の色を切り替える
- バトルログを追加する
- 手の選択ボタンを非表示にする
- ゲーム終了判定を行う

### 14.3 getEnemyHand()

CPUの手をランダムに決定する。

```js
function getEnemyHand() {
  const hands = ["rock", "scissors", "paper"];
  return hands[Math.floor(Math.random() * hands.length)];
}
```

### 14.4 judge(playerHand, enemyHand)

じゃんけんの勝敗を判定する。

返り値：

```js
"win"
"lose"
"draw"
```

判定例：

```js
function judge(playerHand, enemyHand) {
  if (playerHand === enemyHand) {
    return "draw";
  }

  if (
    (playerHand === "rock" && enemyHand === "scissors") ||
    (playerHand === "scissors" && enemyHand === "paper") ||
    (playerHand === "paper" && enemyHand === "rock")
  ) {
    return "win";
  }

  return "lose";
}
```

### 14.5 calculateDamage(result, hand, speedRate)

勝敗、手、反応速度倍率からダメージを計算する。

```js
function calculateDamage(result, hand, speedRate) {
  if (result === "draw") {
    return 0;
  }

  if (result === "miss") {
    return 20;
  }

  const baseDamage = handData[hand].damage;
  return Math.floor(baseDamage * speedRate);
}
```

### 14.6 updateHp(result, damage)

勝敗に応じてHPを更新し、画面表示にも反映する。

```js
function updateHp(result, damage) {
  if (result === "win") {
    enemyHp -= damage;
  } else if (result === "lose" || result === "miss") {
    playerHp -= damage;
  }

  if (playerHp < 0) playerHp = 0;
  if (enemyHp < 0) enemyHp = 0;

  renderHp();
}
```

### 14.7 renderHp()

プレイヤーと敵のHP数値・HPバーを更新する。

```js
function renderHp() {
  $(".player_hp").text(`HP:${playerHp}/100`);
  $(".enemy_hp").text(`HP:${enemyHp}/100`);
  $(".player_hp_bar").css("width", `${playerHp}%`);
  $(".enemy_hp_bar").css("width", `${enemyHp}%`);
}
```

### 14.8 checkGameEnd()

ゲーム終了状態を判定する。

返り値：

```js
"clear"
"gameover"
"continue"
```

### 14.9 表示補助関数

| 関数 | 役割 |
|---|---|
| `getResultLabel(result)` | `win`、`lose`、`draw`、`miss` を画面表示用の日本語に変換する |
| `setResultStyle(result)` | 結果に応じてリザルト欄のクラスを切り替える |
| `getHandImagePath(hand)` | 手に対応する画像パスを返す |
| `resetHandResult()` | 手の画像・ラベル・ダメージ要約を初期化する |
| `showEnemyHandPreview(enemyHand)` | SHOOT時に敵の手だけを先に表示する |
| `showHandResult(playerHand, enemyHand, result, damage)` | プレイヤーと敵の手画像を表示する |
| `showMissResult()` | MISS時の未選択表示を行う |
| `showDamageSummary(result, damage)` | VSの下に与ダメージ・被ダメージを表示する |
| `createBattleLogMessage(result, playerHand, enemyHand, damage)` | 1ラウンド分のログ文を作成する |
| `addBattleLog(message, result)` | 最新のログをバトルログの先頭に追加する |

---

## 15. バトルログ仕様

バトルログは、最新のラウンド結果を上に追加する。

| 結果 | ログ例 |
|---|---|
| 勝ち | 勝ち！あなた:グー / 敵:チョキ。パワーパンチで敵に45ダメージ！ |
| 負け | 負け！あなた:グー / 敵:パー。45ダメージを受けた！ |
| あいこ | あいこ！あなた:グー / 敵:グー。ダメージなし。 |
| MISS | MISS！反応が遅れた！20ダメージを受けた！ |

ゲーム終了後にリスタートした場合、バトルログは空にする。

---

## 16. 使用ファイル

| ファイル | 役割 |
|---|---|
| `janken_tpl.html` | HTML構造、ゲームロジック、jQueryイベント処理 |
| `css/style.css` | 画面レイアウト、HPバー、リザルト、バトルログ、レスポンシブ対応 |
| `js/jquery-2.1.3.min.js` | DOM操作とイベント処理 |
| `images/player.svg` | プレイヤー画像 |
| `images/enemy.svg` | 敵画像 |
| `images/gu.png` | グー画像 |
| `images/choki.png` | チョキ画像 |
| `images/par.png` | パー画像 |

---

## 17. 実装上の注意

- ラウンド中かどうかは `isRoundActive` で管理する
- 1ラウンドで複数回手を選べないように、選択後は `isRoundActive` を `false` にする
- MISS判定のタイマーは、`SHOOT!` 表示から1.0秒後に実行される
- CPUの手は `SHOOT!` 時に `currentEnemyHand` に保存する
- ラウンド終了後は手の選択ボタンを非表示にする
- HPが0以下になった場合は「次のラウンド」ではなく「リスタート」を表示する
- HP表示は数値とバーの両方を更新する
- リザルトとバトルログは、勝ち・負け・あいこ・MISSで表示色を切り替える
